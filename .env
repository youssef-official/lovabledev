# Database Connection
# Replace with your actual database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lovable_clone

# NextAuth Configuration
# You can generate a secret using: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production
NEXTAUTH_URL=http://localhost:3000

# Google OAuth
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# AI Providers
# The project uses Minimax and OpenRouter for code generation.

# OpenRouter API Key (Required for GPT-4, Claude, etc. via OpenRouter)
OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Minimax API Key (Required for Minimax models)
MINIMAX_API_KEY=your-minimax-api-key

# Groq API Key (Optional, used in some analysis routes)
GROQ_API_KEY=gsk_your_groq_key

# E2B Configuration (Required for Sandboxes)
# Get your API key from https://e2b.dev
E2B_API_KEY=e2b_your_api_key

# Firecrawl API Key (Optional, for web scraping capabilities)
FIRECRAWL_API_KEY=fc-your_firecrawl_key

# Note: This project does not directly use OpenAI or Anthropic SDKs with their specific keys.
# All standard models are accessed through OpenRouter.
